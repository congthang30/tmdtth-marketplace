## 0. Tiến độ thực hiện code

Ghi chú: file plan này đang được tái tạo từ trạng thái repository vì trước khi cập nhật file có kích thước 0 byte. Các task dưới đây chỉ được ghi nhận theo phần code và quality gate đã xác minh trong lượt làm việc hiện tại.

| Task ID | Trạng thái | Ngày | Ghi chú |
| --- | --- | --- | --- |
| Build blocker | Hoàn thành | 2026-07-03 | Khôi phục `backend/src/modules/orders/types.ts` về đúng type response cho checkout/order/payment; loại bỏ nội dung `AiModelController` bị ghi nhầm khiến `orders.service.ts` và `payments.service.ts` không build được. `npm.cmd run build`, `npm.cmd test -- --runInBand`, `npm.cmd run test:e2e -- --runInBand`, `npm.cmd run lint`, `npm.cmd audit --omit=dev` đều pass. |
| P1-07 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho active cart: `GET /api/cart` dùng `JwtAuthGuard`, lấy current user từ JWT, tự tạo cart `Active` khi user chưa có cart, chỉ trả item thuộc cart của user, include product/variant/shop/thumbnail/inventory và tính `itemCount`, `selectedItemCount`, `subtotal`, `selectedSubtotal` server-side. Unit test mới cover tự tạo cart rỗng; e2e mocked cover route `GET /api/cart` và current user. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 82/82, `npm.cmd run test:e2e -- --runInBand` 49/49, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P1-08 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho add cart item: `POST /api/cart/items`; DTO validate `productVariantId` dạng số và `quantity` nguyên 1..999; service chỉ cho thêm variant `Active` của product `Published`, shop `Approved`, category active và không deleted/violation; chặn vượt tồn kho; nếu variant đã có trong cart thì cộng quantity thay vì tạo duplicate; snapshot giá chỉ dùng tham khảo và checkout sẽ tính lại. Unit test mới cover add thành công, duplicate increment và out-of-stock; e2e mocked cover route add item và validation quantity lỗi trước service call. Quality gate pass cùng P1-07: build, unit 82/82, e2e 49/49, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P1-09 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho update/select/remove cart item: `PATCH /api/cart/items/:id`, `PATCH /api/cart/items/:id/select`, `DELETE /api/cart/items/:id`; service kiểm tra ownership bằng cart của current user, chặn sửa item user khác, validate quantity không vượt tồn kho, cập nhật `isSelected`, bắt buộc body select có `isSelected`, xóa item thuộc user và touch cart `updatedAt`; item không tồn tại trả `NotFoundException`. Unit test mới cover update quantity/selection, cross-user forbidden, select thiếu `isSelected`, delete owned item và missing item; e2e mocked cover update/select/delete route. Quality gate pass cùng P1-07: build, unit 82/82, e2e 49/49, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P1-10 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho checkout preview: `POST /api/orders/checkout-preview` dùng `JwtAuthGuard`, lấy current user từ JWT, DTO validate `addressId`, `paymentMethodId`, `selectedCartItemIds` unique và optional `voucherCode`; service kiểm tra address thuộc user, payment method active, active cart của user, selected cart item thuộc cart hiện tại, product/variant/shop/category còn purchasable, tồn kho đủ; tính lại giá hiện tại server-side, trả `priceChanged`, group theo shop, subtotal/discount/shipping/total và không tạo order/payment/inventory transaction ở bước preview. Unit test mới cover preview đa shop, current price/priceChanged, ownership address, selected item không thuộc cart, out-of-stock và shop chưa approved; e2e mocked cover route `POST /api/orders/checkout-preview` và validation ID lỗi trước service call. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 87/87, `npm.cmd run test:e2e -- --runInBand` 51/51, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P1-11 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho create order transaction: `POST /api/orders` dùng `JwtAuthGuard`, DTO kế thừa checkout preview và validate `customerNote`; service chạy trong Prisma transaction, đọc lại address/payment/cart từ DB, tính lại giá server-side, tạo 1 `Order`, tách `ShopOrder` theo từng shop, tạo snapshot `OrderItem`, tạo `Payment` pending, ghi `OrderStatusHistory`/`PaymentStatusHistory`, reserve inventory và xóa selected cart item sau khi thành công. Unit test mới cover multi-shop split, snapshot product/variant/SKU/price, payment COD pending, clear cart và atomic stock race fail không tạo payment/không xóa cart; e2e mocked cover `POST /api/orders` happy path và validation customer note. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 98/98, `npm.cmd run test:e2e -- --runInBand` 56/56, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P1-12 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho customer order history: `GET /api/orders/my` dùng `JwtAuthGuard`, pagination chuẩn, service chỉ query `Order` với `userId=currentUser.id`, trả snapshot shipping address, payment method, payments, shop orders, order items, shipments và tracking histories để customer xem trạng thái giao hàng. Unit test mới cover ownership filter, pagination args/meta và shipment tracking summary; e2e mocked cover list route, pagination meta, shipment status/tracking history và current user arg. Quality gate pass cùng P1-11: build, unit 98/98, e2e 56/56, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P1-13 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho customer order detail: `GET /api/orders/:id` dùng `JwtAuthGuard`; service parse order id, query bằng cả `id` và `userId=currentUser.id`, trả `ORDER_NOT_FOUND` nếu order không thuộc customer hoặc không tồn tại, `INVALID_ORDER_ID` nếu id sai định dạng; response dùng snapshot order/item/payment/shipment thay vì truy ngược product hiện tại. Unit test mới cover ownership detail, not found và invalid id trước query; e2e mocked cover detail route, payment info và order item snapshot. Quality gate pass cùng P1-11: build, unit 98/98, e2e 56/56, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P1-14 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho COD/fake online payment: create order tạo payment pending với provider `COD` hoặc `FAKE_ONLINE` theo payment method; thêm coverage `POST /api/payments/:id/fake-success` dùng `JwtAuthGuard`, service chạy transaction, chỉ cho customer sở hữu order đánh dấu payment method `FAKE_ONLINE` đang `Pending` thành `Paid`, set `paidAt/providerTransactionCode`, cập nhật `Order.paymentStatus=Paid` và ghi `PaymentStatusHistory`; chặn payment user khác, COD, non-pending và id sai. Unit test mới cover happy path + history, forbidden ownership, COD bị chặn, non-pending bị chặn và invalid id trước transaction; e2e mocked cover fake-success route. Quality gate pass cùng P1-11: build, unit 98/98, e2e 56/56, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P1-15 | Hoàn thành | 2026-07-03 | Xác minh inventory reserve/deduct cho buying flow: khi `POST /api/orders` thành công, service reserve inventory bằng atomic `ProductInventory.updateMany` với điều kiện `quantityAvailable >= quantity`, tăng `quantityReserved`, giảm `quantityAvailable`, ghi `InventoryTransaction` `RESERVE_ORDER` theo `ORDER_ITEM` và rollback transaction nếu stock race fail; khi shipment delivered/completed ở P3-06, reserved inventory được deduct thành sold (`quantityOnHand -= qty`, `quantityReserved -= qty`), ghi `InventoryTransaction` `COMPLETE_ORDER` và tăng `Product.soldCount`. Unit test mới trong create-order spec cover reserve atomic và race fail không tạo payment/không clear cart; P3-06 tests cover deduct khi complete. Quality gate pass cùng P1-11: build, unit 98/98, e2e 56/56, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P2-01 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho seller register shop: `POST /api/shops` dùng `JwtAuthGuard`, lấy current user từ JWT, DTO trim/validate tên shop, email, phone, địa chỉ và tax code; service tự sinh slug từ `shopName`, chặn slug trùng bằng `SHOP_SLUG_EXISTS`, tạo shop thuộc `ownerUserId=currentUser.id`, mặc định `shopStatus=PendingApproval`, `isDeleted=false`, chưa có `approvedByUserId/approvedAt`; tên không sinh được slug trả `INVALID_SHOP_NAME`. Unit test mới cover create pending approval, duplicate slug và invalid slug; e2e mocked cover route đăng ký shop và validation email lỗi trước service call. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 102/102, `npm.cmd run test:e2e -- --runInBand` 59/59, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P2-02 | Hoàn thành | 2026-07-03 | Hoàn thiện admin approve/reject shop: giữ `PATCH /api/admin/shops/:id/approve`, thêm `PATCH /api/admin/shops/:id/reject`; route admin dùng `JwtAuthGuard`, `RolesGuard`, role `Admin`; reject chỉ áp dụng cho shop `PendingApproval`, cập nhật `shopStatus=Rejected`, actor `approvedByUserId` và timestamp `approvedAt/updatedAt` theo schema hiện có; thêm DTO validate reason tùy chọn; unit test cover approve, reject, reject sai trạng thái và shop deleted/not found; e2e health suite không còn rỗng. Quality gate pass: build, unit test 14/14, e2e 1/1, lint, audit 0 vulnerabilities. |
| P2-03 | Hoàn thành | 2026-07-03 | Xác minh và bổ sung test cho seller product list: `GET /api/seller/products` dùng `JwtAuthGuard`, `RolesGuard`, role `Seller`; service chỉ list product `isDeleted=false` thuộc shop có `ownerUserId=currentUser.id`, `shopStatus=Approved`, `isDeleted=false`, pagination chuẩn, sort `createdAt desc`, include shop/category/images/variants/inventory và tính `thumbnailImage`, `priceMin/priceMax`, `quantityAvailable`. Unit test mới cover ownership/approved-shop filter, pagination skip/take/meta, thumbnail và quantityAvailable; e2e mocked cover list route, pagination meta và current seller arg. Quality gate pass cùng P2-01: build, unit 102/102, e2e 59/59, lint, audit, Prisma validate/generate và Docker config đều pass. |
| P2-04 | Hoàn thành | 2026-07-03 | Thêm `POST /api/seller/products` cho seller tạo product; DTO validate `shopId`, `categoryId`, tên, giá tiền, warranty, weight và status `Draft/Published`; service lấy current user từ JWT, chỉ cho tạo trong shop thuộc seller và đã `Approved`, chỉ nhận category active, tự sinh slug có xử lý tiếng Việt `đ/Đ`, chống trùng `[shopId, slug]`, không tin ownership từ client, mặc định `productStatus=Draft`, ghi `createdByUserId/updatedByUserId`; seller response xử lý product chưa có variant bằng `basePrice` thay vì `Infinity`. Test thêm unit cho happy path, shop không hợp lệ, duplicate slug, compareAtPrice thấp hơn basePrice; e2e mocked cover `POST /api/seller/products` thành công và validation giá lỗi. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 18/18, `npm.cmd run test:e2e -- --runInBand` 3/3, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` ở root và backend pass. |
| P2-05 | Hoàn thành | 2026-07-03 | Thêm `PATCH /api/seller/products/:id` và `DELETE /api/seller/products/:id`; update chỉ cho seller sửa product thuộc shop của mình đã `Approved`, validate category active khi đổi category, regenerate slug khi đổi tên và chặn trùng slug trong cùng shop, validate basePrice/compareAtPrice, cho cập nhật mô tả, brand, warranty, weight và status `Draft/Published`; delete là soft delete với `isDeleted=true`, `deletedAt`, `productStatus=Deleted`, `updatedByUserId/updatedAt`, không xóa dữ liệu lịch sử. Unit test cover update happy path, product ngoài ownership, duplicate slug, soft delete; e2e mocked cover PATCH/DELETE route với current seller. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 22/22, `npm.cmd run test:e2e -- --runInBand` 5/5, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities. |
| P2-06 | Hoàn thành | 2026-07-03 | Thêm Variant CRUD cho seller: `GET /api/seller/products/:productId/variants`, `POST /api/seller/products/:productId/variants`, `PATCH /api/seller/products/:productId/variants/:variantId`, `DELETE /api/seller/products/:productId/variants/:variantId`; mọi route dùng current seller và bắt buộc product thuộc shop của seller đã `Approved`; DTO validate SKU, tên variant, JSON option, giá tiền, weight và status `Active/Inactive`; tạo/update chặn trùng SKU trong cùng product, validate price > 0 và compareAtPrice >= price; delete là soft delete bằng `variantStatus=Inactive` vì schema không có cột `isDeleted`, phù hợp với public/cart/checkout vốn chỉ nhận variant `Active`. Unit test cover create, duplicate SKU, invalid JSON, update, delete inactive; e2e mocked cover list/create/validation/update/delete variant. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 27/27, `npm.cmd run test:e2e -- --runInBand` 10/10, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P2-07 | Hoàn thành | 2026-07-03 | Thêm Product image manager cho seller: `GET /api/seller/products/:productId/images`, `POST /api/seller/products/:productId/images`, `PATCH /api/seller/products/:productId/images/:imageId`, `DELETE /api/seller/products/:productId/images/:imageId`; mọi route kiểm tra product thuộc shop current seller đã `Approved`, image thuộc đúng product và optional `productVariantId` thuộc đúng product; DTO validate URL ảnh chỉ nhận `/uploads/...` hoặc `http(s)://...`, alt text, sortOrder, isThumbnail; create/update thumbnail dùng transaction reset thumbnail khác trong cùng product; delete xóa record `ProductImage` nhưng không đụng file upload vật lý. Unit test cover list, create thumbnail reset, update thumbnail reset, delete và ownership failure; e2e mocked cover list/create/validation/update/delete image. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 32/32, `npm.cmd run test:e2e -- --runInBand` 15/15, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P2-08 | Hoàn thành | 2026-07-03 | Thêm inventory management cho seller theo variant: `GET /api/seller/products/:productId/variants/:variantId/inventory` và `PATCH /api/seller/products/:productId/variants/:variantId/inventory`; route kiểm tra product/variant thuộc shop current seller đã `Approved`; DTO validate `quantityOnHand` và `lowStockThreshold`; nếu chưa có inventory thì GET trả snapshot mặc định 0, PATCH tạo record; nếu đã có thì PATCH cập nhật `quantityOnHand`, `quantityAvailable = quantityOnHand - quantityReserved`, giữ hoặc cập nhật `lowStockThreshold`; chặn `quantityOnHand < quantityReserved`; toàn bộ set inventory chạy trong transaction và ghi `InventoryTransaction` với `SELLER_SET_STOCK`, `quantityChange`, `quantityAfter`, `referenceType=PRODUCT_VARIANT`, `createdByUserId`. Unit test cover default inventory, create inventory + transaction log, reject dưới reserved; e2e mocked cover GET/PATCH và validation quantity âm. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 35/35, `npm.cmd run test:e2e -- --runInBand` 18/18, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P2-09 | Hoàn thành | 2026-07-03 | Thêm seller order list/detail: `GET /api/seller/orders` và `GET /api/seller/orders/:id`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Seller`; service chỉ truy vấn `ShopOrder` thuộc shop có `ownerUserId=currentUser.id` và `isDeleted=false`, trả pagination cho list, snapshot order/shipping/payment, shop summary và `OrderItem` snapshot; detail trả `SHOP_ORDER_NOT_FOUND` khi không thuộc seller hoặc ID không tồn tại, `INVALID_SHOP_ORDER_ID` khi ID không hợp lệ. Unit test cover list ownership, detail ownership, not found và invalid ID; e2e mocked cover list/detail route với current seller. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 39/39, `npm.cmd run test:e2e -- --runInBand` 20/20, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P2-10 | Hoàn thành | 2026-07-03 | Thêm seller confirm shop order: `PATCH /api/seller/orders/:id/confirm`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Seller` và DTO validate `sellerNote` tối đa 1000 ký tự; service chạy trong transaction, chỉ cho seller sở hữu shop chuyển `ShopOrder` từ `WaitingForSeller` sang `Confirmed`, set `confirmedAt/updatedAt`, optional `sellerNote`, ghi `OrderStatusHistory`; nếu tất cả shop order của order tổng đã hết `WaitingForSeller` thì cập nhật `Order.orderStatus=Confirmed` và ghi history cho order tổng; chặn order không thuộc seller bằng `SHOP_ORDER_NOT_FOUND`, chặn transition sai bằng `SHOP_ORDER_INVALID_STATUS`. Unit test cover confirm happy path + history, multi-shop còn pending không update order tổng, invalid transition; e2e mocked cover route confirm và validation seller note. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 42/42, `npm.cmd run test:e2e -- --runInBand` 22/22, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P2-11 | Hoàn thành | 2026-07-03 | Thêm seller prepare shop order: `PATCH /api/seller/orders/:id/prepare`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Seller` và DTO validate `sellerNote` tối đa 1000 ký tự; service chạy trong transaction, chỉ cho seller sở hữu shop chuyển `ShopOrder` từ `Confirmed` sang `Prepared`, set `preparedAt/updatedAt`, optional `sellerNote`, ghi `OrderStatusHistory`; nếu tất cả shop order của order tổng đã qua `WaitingForSeller/Confirmed` thì cập nhật `Order.orderStatus=Prepared` và ghi history cho order tổng, làm tiền đề tạo shipment ở phase shipping; chặn order không thuộc seller bằng `SHOP_ORDER_NOT_FOUND`, chặn transition sai bằng `SHOP_ORDER_INVALID_STATUS`. Unit test cover prepare happy path + history, multi-shop còn unprepared không update order tổng, invalid transition; e2e mocked cover route prepare và validation seller note. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 45/45, `npm.cmd run test:e2e -- --runInBand` 24/24, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P3-01 | Hoàn thành | 2026-07-03 | Thêm `ShippingModule` và admin shipping company CRUD: `GET /api/admin/shipping-companies`, `GET /api/admin/shipping-companies/:id`, `POST /api/admin/shipping-companies`, `PATCH /api/admin/shipping-companies/:id`, `DELETE /api/admin/shipping-companies/:id`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Admin`; list có pagination và chỉ trả company chưa `isDeleted`; create gắn `ownerUserId` theo current admin, mặc định `companyStatus=Approved`, set `approvedByUserId/approvedAt`; update validate slug/status/contact fields, chặn slug trùng; delete là soft delete với `isDeleted=true`, `deletedAt`, `companyStatus=Inactive` để không phá lịch sử shipment/shop order. Unit test cover list pagination, create approved owner, duplicate slug, approve metadata, soft delete, deleted detail not found; e2e mocked cover list/create/validation/update/delete route. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 51/51, `npm.cmd run test:e2e -- --runInBand` 29/29, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P3-02 | Hoàn thành | 2026-07-03 | Thêm admin shipping service CRUD: `GET /api/admin/shipping-services`, `GET /api/admin/shipping-services/:id`, `POST /api/admin/shipping-services`, `PATCH /api/admin/shipping-services/:id`, `DELETE /api/admin/shipping-services/:id`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Admin`; list có pagination và filter `shippingCompanyId`; create/update validate company id, service code, money, ETA days, active flag; chỉ cho tạo/chuyển service sang shipping company chưa deleted và `Approved`; chặn trùng unique `(shippingCompanyId, serviceCode)`; chặn `estimatedMinDays > estimatedMaxDays`; delete là deactivate `isActive=false` vì schema không có `isDeleted`. Unit test cover list filter, create service, duplicate code, company chưa approved, ETA invalid, deactivate; e2e mocked cover list/create/validation/update/deactivate route. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 57/57, `npm.cmd run test:e2e -- --runInBand` 34/34, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P3-03 | Hoàn thành | 2026-07-03 | Thêm shipping quote: `POST /api/shipping/quotes`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Customer`; DTO validate `shopId`, `shippingServiceId`, tỉnh/huyện giao hàng và `totalWeightGram`; service chỉ tạo quote cho shop chưa deleted và `Approved`, shipping service `isActive=true`, shipping company chưa deleted và `Approved`; phí được tính server-side bằng `baseFee + ceil(totalWeightGram/1000) * feePerKg`, không tin giá từ client; quote được lưu vào `ShippingQuote` với `expiresAt` sau 30 phút, snapshot ETA và địa chỉ đích. Unit test cover tính phí/tạo quote/expiresAt, shop chưa approved, service inactive; e2e mocked cover tạo quote và validation trọng lượng. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 60/60, `npm.cmd run test:e2e -- --runInBand` 36/36, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P3-04 | Hoàn thành | 2026-07-03 | Thêm seller create shipment: `POST /api/seller/orders/:shopOrderId/shipments`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Seller`; DTO validate `shippingServiceId`, optional `shippingQuoteId`, tracking number, pickup address, expected delivery date và note; service chạy trong transaction, chỉ seller sở hữu shop order được tạo shipment từ `ShopOrder` đang `Prepared`, chặn shipment trùng trong MVP, bắt shipping service active và shipping company `Approved`; nếu có quote thì quote phải đúng shop/service/company và chưa hết hạn, nếu không có quote thì tính phí từ trọng lượng variant trong order items; tự tạo `Shipment`, toàn bộ `ShipmentItem` theo order items, `ShipmentTrackingHistory` trạng thái `Pending`, cập nhật `ShopOrder` sang `Shipping`, gắn shipping company/service/quote/fee và ghi `OrderStatusHistory`; nếu mọi shop order của order tổng đã qua trạng thái trước shipping thì cập nhật `Order.orderStatus=Shipping`. Unit test cover happy path transaction + item/history/status, invalid shop order status, quote expired; e2e mocked cover route create shipment và validation tracking number. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 63/63, `npm.cmd run test:e2e -- --runInBand` 38/38, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `docker compose config` pass. |
| P3-05 | Hoàn thành | 2026-07-03 | Thêm seller tracking update: `PATCH /api/seller/orders/:shopOrderId/shipments/:shipmentId/tracking`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Seller`; DTO validate `shipmentStatus` chỉ nhận `PickedUp/InTransit/Delivered`, tracking number, location và note; service chạy transaction, chỉ seller sở hữu shop order được cập nhật shipment, chặn transition sai bằng allowlist `Pending -> PickedUp/InTransit -> Delivered`, ghi `ShipmentTrackingHistory`, set `pickedUpAt/deliveredAt`, và khi shipment `Delivered` thì cập nhật `ShopOrder.orderStatus=Delivered`, cập nhật `Order.orderStatus=Delivered` khi toàn bộ shop order đã delivered/completed, kèm `OrderStatusHistory`; customer order response include shipment summary và tracking histories để xem trạng thái giao hàng. Unit test cover tracking update, delivered propagation và invalid transition; e2e mocked cover route tracking và validation status. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 66/66, `npm.cmd run test:e2e -- --runInBand` 40/40, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P3-06 | Hoàn thành | 2026-07-03 | Hoàn tất order sau giao hàng trong cùng transaction tracking: khi shipment chuyển `Delivered` và mọi shipment của shop order đã delivered, hệ thống ghi `ShopOrder` qua `Delivered` rồi `Completed`, set `completedAt`, ghi `OrderStatusHistory`; khấu trừ reserved inventory thành sold bằng `ProductInventory.quantityOnHand -= quantity`, `quantityReserved -= quantity`, giữ invariant `quantityAvailable = quantityOnHand - quantityReserved`, ghi `InventoryTransaction` `COMPLETE_ORDER` theo `ORDER_ITEM`, tăng `Product.soldCount`; khi toàn bộ shop order của parent order đã `Completed`, hệ thống ghi `Order` qua `Delivered` rồi `Completed`, set `completedAt` và history. Unit test cover completion happy path, inventory deduction, product sold count, shop/order status histories và invalid shipment transition vẫn bị chặn; e2e tracking route giữ coverage HTTP/validation. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 66/66, `npm.cmd run test:e2e -- --runInBand` 40/40, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P4-01 | Hoàn thành | 2026-07-03 | Thêm `ReviewsModule` cho customer tạo product review: `POST /api/reviews/products`; route dùng `JwtAuthGuard`, `RolesGuard`, role `Customer`; DTO validate `orderItemId`, rating nguyên 1-5, title tối đa 255 và content tối đa 2000 ký tự; service lấy current user từ JWT, chỉ cho review `OrderItem` thuộc order của user, chỉ khi `Order` hoặc `ShopOrder` đã `Delivered/Completed`, tự snapshot `productId/productVariantId` từ order item, mặc định `reviewStatus=Published`, chặn duplicate bằng unique `(orderItemId,userId)` với lỗi `PRODUCT_REVIEW_ALREADY_EXISTS`, không nhận ownership từ client. Unit test cover happy path, order item user khác, order chưa hoàn tất, duplicate review và id không hợp lệ; e2e mocked cover route tạo review và validation rating. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 71/71, `npm.cmd run test:e2e -- --runInBand` 42/42, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P4-02 | Hoàn thành | 2026-07-03 | Thêm public product reviews: `GET /api/products/:slug/reviews`; route public không yêu cầu token, dùng pagination chuẩn; service chỉ trả review `Published` của product public (`productStatus=Published`, chưa deleted/violation, shop `Approved`), trả reviewer display name từ `UserProfile.fullName` hoặc `Customer`, variant summary và nội dung review, không lộ email/phone/user internals; product không public trả `PRODUCT_NOT_FOUND`. Unit test cover list pagination, filter product/shop public, published-only where, reviewer display name và product không public; e2e mocked cover list route và pagination meta. Quality gate pass: `npm.cmd run build`, `npm.cmd test -- --runInBand` 73/73, `npm.cmd run test:e2e -- --runInBand` 43/43, `npm.cmd run lint`, `npm.cmd audit --omit=dev` 0 vulnerabilities, `npm.cmd run prisma:validate`, `npm.cmd run prisma:generate`, `docker compose config` ở root và backend pass. |
| P2-12 | Hoàn thành | 2026-07-03 | Thêm customer cancellation cơ bản qua `PATCH /api/orders/:id/cancel`; DTO bắt buộc reason đã trim, tối đa 1000 ký tự; chỉ customer sở hữu order được hủy khi parent còn `Created`, mọi `ShopOrder` còn `WaitingForSeller` và mọi payment còn `Pending`. Transaction dùng conditional update để chặn race với seller/payment, chuyển order/shop orders/payment sang `Cancelled`, tạo `OrderCancellation` approved, ghi `OrderStatusHistory`/`PaymentStatusHistory`, hoàn `quantityReserved` về `quantityAvailable` cho từng order item và ghi `InventoryTransaction` loại `RELEASE_ORDER`; mọi lỗi inventory/payment/status rollback toàn bộ. Unit test cover multi-shop release, ownership isolation, confirmed order, paid payment, thiếu reserved inventory và invalid id; HTTP e2e cover happy path và reason rỗng. Quality gate pass: build, unit 108/108, e2e 61/61, lint, audit 0 vulnerabilities, Prisma validate/generate. PostgreSQL thật/migration/seed chưa xác minh trong lượt này vì Docker không được cài và localhost:5432 từ chối kết nối. |
| Security E2E hardening | Hoàn thành | 2026-07-03 | Thêm `test/auth-authorization.e2e-spec.ts` dùng `JwtAuthGuard` và `RolesGuard` thật (không override guard): kiểm tra 401 trên toàn bộ 64 protected HTTP routes; kiểm tra 403 trên mọi seller/admin route và các customer-only shipping/review routes; cover malformed/invalid token, token trỏ user không tồn tại, actor injection đúng và happy path đại diện Customer/Seller/Admin. Guard chặn request trước validation/service khi sai role. Quality gate pass: build, unit 108/108, E2E 174/174, lint, Prisma validate, audit 0 vulnerabilities. |
| Embedded PostgreSQL + payment discovery | Hoàn thành | 2026-07-03 | Thêm dev dependency `embedded-postgres` và `npm run db:embedded` để chạy PostgreSQL UTF-8 cục bộ ở port 55432 khi không có Docker; `.tmp/` được ignore và README có hướng dẫn. Đã chạy thật `db:check`, apply đủ 2 migration, seed hai lần chứng minh idempotent, `db:seed:check` pass với 4 users/17 categories/2 shipping companies/3 services/2 products/3 variants. Thêm authenticated `GET /api/payments/methods` để checkout client lấy COD/FAKE_ONLINE active thay vì phải biết ID nội bộ; unit và E2E cover active-only/response. Health, customer login và endpoint payment methods đã gọi thành công trên NestJS + Prisma + PostgreSQL thật. Quality gate pass: build, unit 109/109, E2E 176/176, lint, Prisma validate, audit 0 vulnerabilities. |
| Final MVP acceptance | Hoàn thành | 2026-07-03 | Thêm `scripts/mvp-acceptance.mjs` và `npm run test:mvp`; đã chạy thành công qua NestJS + Prisma + PostgreSQL thật: admin login/tạo category; đăng ký Seller A/B và customer; tạo/approve 2 shop; mỗi seller tạo product + variant + inventory; customer tạo address, lấy payment method, add cart 2 shop, preview và create order; xác minh 1 parent order tách đúng 2 `ShopOrder`; cả hai seller confirm/prepare/create shipment/update InTransit/Delivered; inventory được hoàn tất qua production service; parent order đạt `Completed`; customer review thành công và duplicate review bị chặn. Kết quả thật: `{ok:true, orderId:"2", shopOrders:2, status:"Completed", duplicateReviewBlocked:true}`. Negative/race/ownership/rollback cases được cover bởi 109 unit tests và 176 HTTP E2E tests. Build, lint, audit, Prisma validate/migrate status, seed idempotency đều pass. `MVP COMPLETE`. |
| MVP re-verification | Hoàn thành | 2026-07-03 | Chạy lại quality gate trên repository hiện tại sau yêu cầu tiếp tục task: `npm.cmd run build` pass; `npm.cmd test -- --runInBand` pass 109/109; `npm.cmd run test:e2e -- --runInBand` pass 176/176; `npm.cmd run lint` pass; `npm.cmd audit --omit=dev` pass 0 vulnerabilities sau khi cho phép truy cập npm registry; `npm.cmd run prisma:validate` và `npm.cmd run prisma:generate` pass; embedded PostgreSQL `localhost:55432` pass `db:check` với 44 table, `npx.cmd prisma migrate status` báo database up to date với 2 migrations, `npm.cmd run db:seed` và `npm.cmd run db:seed:check` pass; NestJS chạy ở `PORT=3100` pass health endpoint và `npm.cmd run test:mvp` trả `{ok:true, orderId:"34", shopOrders:2, status:"Completed", duplicateReviewBlocked:true}`. `docker compose config` ở root và backend exit 0, có cảnh báo quyền đọc `C:\Users\Cong Thang\.docker\config.json` nhưng config render đầy đủ. Không phát hiện task MVP Must-have còn thiếu cần code thêm. |
