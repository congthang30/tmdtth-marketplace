export const routes = {
  home: '/',
  products: '/products',
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  seller: '/seller',
  admin: '/admin',
  forbidden: '/forbidden',
} as const;
